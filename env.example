# Example environment configuration for JARVIS

# -----------------------------------------------------------------------------
# Database configuration
#
# The database used by JARVIS is configured via the SQLAlchemy URL below.
# For local development on Windows a SQLite file is recommended.  When using
# SQLite the database file will be created automatically if it does not
# exist.  To use Postgres/TimescaleDB in a production environment, set
# DATABASE_URL to a suitable connection string using the psycopg2 driver.

# Local SQLite database (default for quickstart)
DATABASE_URL=sqlite:///./jarvis.db

# Example Postgres connection (uncomment to use)
#DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5432/jarvis


# -----------------------------------------------------------------------------
# Alpaca API configuration (optional)
#
# These keys are required only when ingesting live data from Alpaca.  Leave
# unset or blank when running the system offline or in test mode.
#ALPACA_API_KEY_ID=your_key
#ALPACA_API_SECRET_KEY=your_secret


# -----------------------------------------------------------------------------
# LLM configuration (optional)
#
# By default the pipeline uses a deterministic FakeLLMClient.  To enable
# integration with real large language models set LLM_MODE to "live" and
# provide the appropriate API keys.  Only OpenAI and Google Gemini are
# supported in Phase 2; additional providers will be rejected.
LLM_MODE=fake
#OPENAI_API_KEY=your_openai_key
#GEMINI_API_KEY=your_gemini_key
#GOOGLE_API_KEY=your_google_key


# -----------------------------------------------------------------------------
# Telegram notification configuration (optional)
#
# When notification is enabled, JARVIS will send Telegram messages only when
# a trade decision is returned.  To configure notifications, set the bot
# token, the target chat ID and an allowlist of permitted chat IDs.  You can
# also customise the directory used to store idempotency keys.
#TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234
#TELEGRAM_CHAT_ID=123456789
#TELEGRAM_CHAT_ID_ALLOWLIST=123456789
#TELEGRAM_STATE_DIR=artifacts/state


# -----------------------------------------------------------------------------
# Scheduling configuration (optional)
#
# These settings control the Phase 3 daily scan schedule when using the
# bundled scheduler (e.g. via Docker).  They are not required for local
# operation.
#JARVIS_SCAN_TZ=America/New_York
#JARVIS_SCAN_CRON=15 10 * * 1-5